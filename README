Кастом-поле, упрощающее построение отчетов, связанных с версиями.

После добавления поля к определенному типу issue (или ко всем issues) необходимо выполнить Configure -> Options и добавить в качестве опции строку, равную префиксу имени версии, которая интересует. Например: "Фаза разработки".

foreach version in (thisProject.getVersions()) {
  if(theVersion.isPrefixOf(version.getName())) {
    if ( version.isReleased() ) {
      зеленая галочка
    } else if (version.dueDate () > today () ) {
       if (thisProject.hasOverdueTasksAffectingThisVersion()) {
         желтый кружочек
       } else {
         зеленый кружочек
       }
    } else {
       красный кружочек
    }
  }
}
